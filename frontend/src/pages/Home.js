import React from 'react';
import Calendar from '../components/Calendar';

const HomePage = () => (
  <div className="home-container">
    <h1 className='home-title'>Reservation</h1>
    <Calendar />
  </div>
);

export default HomePage;